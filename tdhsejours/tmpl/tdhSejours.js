/*jQuery(document).ready(function() {
    console.log( "ready TDHSejours!" );
	
	// récupération des aéroports de départ
	jQuery.get(

    'http://www.speedresa.com/lib/xml.php?AGENCE=T15&list=aeroports', // Le fichier cible côté serveur.

    'false', // Nous utilisons false, pour dire que nous n'envoyons pas de données.

    'ajax_aeroport', // Nous renseignons uniquement le nom de la fonction de retour.

    'xml' // Format des données reçues.

);


function ajax_aeroport(texte_recu){

    // Du code pour gérer le retour de l'appel AJAX.
	alert("récupération de la liste : "+texte_recu);

}


	
    jQuery(".js-example-basic-single").select2();
});
*/